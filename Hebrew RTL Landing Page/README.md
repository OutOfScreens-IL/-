# מחוץ למסכים — אתר רשמי

אתר שיווקי מלא לחברת **מחוץ למסכים**, המתמחה בפעילויות ODT וימי שיא לבתי ספר.

## טכנולוגיות

- **React 19** + **TypeScript 5.7**
- **Vite 8**
- **Tailwind CSS v4** (via `@tailwindcss/vite`)
- **react-router v8** (Data mode — `createBrowserRouter`)
- **Supabase** (Edge Functions + PostgreSQL)
- גופן: **Assistant** מ-Google Fonts
- RTL מלא בעברית (`dir="rtl"`)

## מבנה הפרויקט

```
src/
├── App.tsx                    # נקודת כניסה — RouterProvider
├── routes.ts                  # הגדרת ראוטים
├── shared.tsx                 # טוקנים, hooks, אייקונים, CSS גלובלי
├── index.css                  # Tailwind + גופן + RTL
├── components/
│   └── SiteLayout.tsx         # Header (המבגר מובייל) + Footer + Outlet
└── pages/
    ├── Home.tsx               # דף הבית
    ├── About.tsx              # עלינו
    ├── Activities.tsx         # הפעילויות
    └── Contact.tsx            # צרו קשר

supabase/
└── functions/server/
    ├── index.tsx              # Hono Edge Function — /contact-submit
    └── kv_store.tsx           # Supabase KV client (autogenerated)

utils/supabase/
└── info.tsx                   # projectId + publicAnonKey (autogenerated)
```

## דפים

| דף | נתיב | תיאור |
|---|---|---|
| בית | `/` | הירו, מיומנויות, שלבים, שכבות גיל, CTA |
| הפעילויות | `/activities` | 3 סוגי פעילות, שכבות גיל, CTA |
| עלינו | `/about` | מייסדות, סיפור, הקדשה, גלריית תחנות |
| צרו קשר | `/contact` | ירו + כרטיסי קשר (WhatsApp/טלפון/מייל/Instagram) |

## צבעי המותג

| שם | HEX |
|---|---|
| CORAL | `#F15746` |
| YELLOW | `#FFC713` |
| GREEN | `#608E65` |
| BLUE | `#536B8E` |
| BLACK | `#0C0C0C` |
| PAGE_BG | `#F2F1ED` |

## פרטי יצירת קשר

- 📞 054-2301160
- 📧 out.of.the.screen1@gmail.com
- 📷 [@outofthescreen_](https://instagram.com/outofthescreen_)
- 💬 [WhatsApp](https://wa.me/972542301160)

## Supabase

- **Project ID:** `ssvpbfgqlgplgbvihwkc`
- **טבלה:** `submissions` — שמירת פניות מהאתר
- **Edge Function:** `make-server-3e74026d` — endpoint: `POST /contact-submit`

### SQL ליצירת הטבלה

```sql
create table if not exists submissions (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  phone text not null,
  email text not null,
  school text,
  role text,
  city text,
  grade text,
  participants text,
  preferred_date text,
  focus text,
  message text,
  created_at timestamptz default now()
);

alter table submissions enable row level security;

create policy "Allow anon insert"
  on submissions for insert
  to anon
  with check (true);
```

## הרצה מקומית

```bash
pnpm install
pnpm dev
```

האתר יעלה על `http://localhost:5173`

## בנייה לייצור

```bash
pnpm build
```
